# CSCE4050Crypt - Group 15
#The purpose of the GitHub is for CSCE 4050 Project. 
#Firet set up two python program, server.py (containing weather message with http server) and client.py (GET message from server and run server http) 

#To run programs: 
#Task one
# 1. Open terminal and type pip install flask
    python3 -m venv venv
    source venv/bin/activate
    python3 -m pip install flask
    python3 -m pip install requests
# 2. Type python3 server.py
# 3. Open another terminal and type pip install request, then type python3 client.py
# 4. A default web browser (Google, Edge, or Etc.) will open to a http page containing weather information of Denton. 
# Task 2 
# Open repository on terminal or vs code terminal
# Type the following: 
    python3 -m venv venv
    source venv/bin/activate
    python3 -m pip install flask
    python3 -m pip install requests
    python3 -m pip install pycryptomdome 
# then type server2.py for the first 6 steps in task 2
# Open another terminal for client2.py
# Type the same collection if not in (venu)
    python3 -m venv venv
    source venv/bin/activate
    python3 -m pip install flask
    python3 -m pip install requests
    python3 -m pip install pycryptomdome 
# then type client2.py
# for the last step one terminal should be able to run server_upd.py and client_upd.py on the other. Close server_upd.py and run server_superfake.py and client_upd.py to get see the results for step 2.8

# To watch the programs run here is our video below
# YouTube videolink: https://youtu.be/MoznYlKrdrw